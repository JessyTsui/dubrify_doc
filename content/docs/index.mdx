---
title: Dubrify API
description: 企业级 AI 模型统一接入平台，提供稳定、高效、经济的 AI 能力接入解决方案
---

import { Card, Cards } from 'fumadocs-ui/components/card';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';

# Dubrify API

企业级 AI 模型统一接入平台 — 一套 API 标准访问全球顶尖 AI 模型。

<Cards>
  <Card title="全模型覆盖" description="GPT、Claude、Gemini、DALL-E、Sora、Grok Video 等主流模型" />
  <Card title="极简接入" description="完全兼容 OpenAI SDK，只需更换 API endpoint" />
  <Card title="视频生成" description="支持 Sora 和 Grok Imagine Video 文生视频、图生视频" />
  <Card title="企业级保障" description="全球多地部署，7x24 服务监控" />
</Cards>

## 快速开始

只需两步即可开始使用：

**1. 替换接口地址**

```
https://api.openai.com → https://api.dubrify.com
```

**2. 获取 API 密钥**

访问 [ai.dubrify.com](https://ai.dubrify.com) 创建您的 API Key。

## 代码示例

<Tabs items={['Python', 'JavaScript']}>
  <Tab value="Python">
```python
from openai import OpenAI

client = OpenAI(
    base_url="https://api.dubrify.com/v1",
    api_key="your-api-key"
)

response = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=[{"role": "user", "content": "Hello, Dubrify!"}]
)
```
  </Tab>
  <Tab value="JavaScript">
```javascript
import OpenAI from 'openai';

const client = new OpenAI({
    baseURL: 'https://api.dubrify.com/v1',
    apiKey: 'your-api-key'
});

const response = await client.chat.completions.create({
    model: 'gpt-4o-mini',
    messages: [{ role: 'user', content: 'Hello, Dubrify!' }]
});
```
  </Tab>
</Tabs>

## 如何选择入口

首次接入建议先看**使用指南**（概念、步骤、示例）；当你需要按接口字段查参数或直接试调请求时，请进入**接口调试**。

## 文档导航

<Cards>
  <Card title="接口调试" href="/api-reference" description="基于 OpenAPI 的交互式接口文档，可直接调试请求并查看响应结构" />
  <Card title="使用指南" href="/docs/guide" description="API 接口使用说明，模型列表和高级功能" />
  <Card title="Claude Code 配置" href="/docs/cli-setup" description="使用 CC-Switch 配置 Claude Code / Codex" />
  <Card title="视频生成" href="/docs/video" description="Sora 和 Grok Video 视频生成完整指南" />
  <Card title="代码示例" href="/docs/examples" description="Python / JavaScript / Go / cURL 多语言示例" />
</Cards>
